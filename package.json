{
  "name": "homebridge-gpio-switch-dualrelay",
  "version": "0.1.5",
  "description": "For a switch that requires two GPIO pins (closing pin and opening pin), and requires them to be active for some amount of time.",
  "preferGlobal": true,
  "keywords": [
    "homebridge-plugin",
    "homebridge",
    "garagedoor",
    "raspberry",
    "pi",
    "gpio",
    "relay"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/castorverhoog/homebridge-gpio-switch-dualrelay.git"
  },
  "engines": {
    "node": ">=0.12.0",
    "homebridge": ">=0.2.0"
  },
  "dependencies": {
    "onoff": "^6.0.0"
  }
}
